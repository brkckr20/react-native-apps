import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container : {
        padding : 5,
        margin : 10,
        backgroundColor : "#e0e0e0",
        borderRadius : 5
    }
})