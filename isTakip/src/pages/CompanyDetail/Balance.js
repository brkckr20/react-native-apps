import React from 'react';
import { Text, View } from 'react-native';
import styles from './styles';

const Balance = () => {
    return (
        <View style={styles.sendProductContainer}>
            <Text style={styles.infoText}>Bakiye Bilgileri</Text>
        </View>
    )
}

export default Balance;